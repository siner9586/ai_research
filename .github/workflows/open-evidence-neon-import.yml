name: open-evidence-neon-import-v1-legacy

on:
  workflow_dispatch:

permissions:
  id-token: write
  actions: read
  contents: read

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Completed v1 staging notice
        run: |
          echo 'The v1 rescue and PubMed candidate staging run has completed and been merged.'
          echo 'This workflow is retained for audit history and manual disaster recovery only.'
